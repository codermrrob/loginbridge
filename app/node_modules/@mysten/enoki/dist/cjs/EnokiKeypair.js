"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __accessCheck = (obj, member, msg) => {
  if (!member.has(obj))
    throw TypeError("Cannot " + msg);
};
var __privateGet = (obj, member, getter) => {
  __accessCheck(obj, member, "read from private field");
  return getter ? getter.call(obj) : member.get(obj);
};
var __privateAdd = (obj, member, value) => {
  if (member.has(obj))
    throw TypeError("Cannot add the same private member more than once");
  member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
};
var __privateSet = (obj, member, value, setter) => {
  __accessCheck(obj, member, "write to private field");
  setter ? setter.call(obj, value) : member.set(obj, value);
  return value;
};
var EnokiKeypair_exports = {};
__export(EnokiKeypair_exports, {
  EnokiKeypair: () => EnokiKeypair,
  EnokiPublicKey: () => EnokiPublicKey
});
module.exports = __toCommonJS(EnokiKeypair_exports);
var import_cryptography = require("@mysten/sui.js/cryptography");
var import_zklogin = require("@mysten/zklogin");
var _address, _ephemeralPublicKey, _proof, _maxEpoch, _ephemeralKeypair, _publicKey;
class EnokiPublicKey extends import_cryptography.PublicKey {
  constructor(input) {
    super();
    __privateAdd(this, _address, void 0);
    __privateAdd(this, _ephemeralPublicKey, void 0);
    __privateSet(this, _address, input.address);
    __privateSet(this, _ephemeralPublicKey, input.ephemeralPublicKey);
  }
  flag() {
    return import_cryptography.SIGNATURE_SCHEME_TO_FLAG["ZkLogin"];
  }
  toSuiAddress() {
    return __privateGet(this, _address);
  }
  verify() {
    throw new Error("Verification for EnokiPublicKey is not supported");
  }
  toRawBytes() {
    return __privateGet(this, _ephemeralPublicKey).toRawBytes();
  }
}
_address = new WeakMap();
_ephemeralPublicKey = new WeakMap();
class EnokiKeypair extends import_cryptography.Keypair {
  constructor(input) {
    super();
    __privateAdd(this, _proof, void 0);
    __privateAdd(this, _maxEpoch, void 0);
    __privateAdd(this, _ephemeralKeypair, void 0);
    __privateAdd(this, _publicKey, void 0);
    __privateSet(this, _proof, input.proof);
    __privateSet(this, _maxEpoch, input.maxEpoch);
    __privateSet(this, _ephemeralKeypair, input.ephemeralKeypair);
    __privateSet(this, _publicKey, new EnokiPublicKey({
      address: input.address,
      ephemeralPublicKey: input.ephemeralKeypair.getPublicKey()
    }));
  }
  signData(data) {
    return __privateGet(this, _ephemeralKeypair).signData(data);
  }
  async sign(data) {
    return __privateGet(this, _ephemeralKeypair).sign(data);
  }
  signPersonalMessage() {
    throw new Error("Signing personal messages is not supported");
  }
  async signTransactionBlock(bytes) {
    const { bytes: signedBytes, signature: userSignature } = await __privateGet(this, _ephemeralKeypair).signTransactionBlock(bytes);
    const zkSignature = (0, import_zklogin.getZkLoginSignature)({
      inputs: __privateGet(this, _proof),
      maxEpoch: __privateGet(this, _maxEpoch),
      userSignature
    });
    return {
      bytes: signedBytes,
      signature: zkSignature
    };
  }
  getKeyScheme() {
    return __privateGet(this, _ephemeralKeypair).getKeyScheme();
  }
  getPublicKey() {
    return __privateGet(this, _publicKey);
  }
  export() {
    throw new Error("EnokiKeypair does not support exporting");
  }
  getSecretKey() {
    throw new Error("EnokiKeypair does not support get secret key");
  }
}
_proof = new WeakMap();
_maxEpoch = new WeakMap();
_ephemeralKeypair = new WeakMap();
_publicKey = new WeakMap();
//# sourceMappingURL=EnokiKeypair.js.map
